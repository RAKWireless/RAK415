PC端命令配置工具 更新至---V1.0.1，更新如下：

1.读取模块MAC不缺省。如60:C5:A8:00:00:03，不再是60:C5:A8:0:0:3。
2.模块扫描路由器列表支持中文显示。
3.保存认证信息命令添加数据长度，否则保存不成功。
4.导入导出cfg配置文件支持中文读写。
5.保存或读取模块的出厂配置和用户配置支持中文。

PC端命令配置工具 更新至---V1.2，更新如下：
1.修改背景。